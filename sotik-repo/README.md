# sait sotika

Статический сайт, собранный с помощью Vite.

## Команды

- `npm install` — установить зависимости
- `npm run build` — собрать сайт
- `npm run dev` — запустить локальный сервер разработки
